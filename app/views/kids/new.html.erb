<h1>Create a New Kid</h1>

<%= render partial: "layouts/error", locals: {object: @kid} %>
<%= form_for (@kid) do |f|%>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :birthday %>
    <%= f.date_field :birthday%>

    <%= f.submit "Add Kid"%>
<% end %>